import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  header: {
    width: '100%',
    flexDirection: 'row',
    paddingTop: 40,
    paddingBottom: 20,
    paddingHorizontal: 20,
    marginBottom: 20,
    backgroundColor: '#eed5d5',
    alignItems: 'center',
    justifyContent: 'space-between',
  },
});